<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moonback.pilgrimage.model.mapper.MemberMapper">
	<select id="findByEmail"
			parameterType="string"
			resultType="com.moonback.pilgrimage.model.dto.MemberDto">
		SELECT
            member_id,
            email,
            password,
            nickname,
            role_id,
            status_id,
            created_at
        FROM member
        WHERE email = #{email}
	</select>
	
	<select id="findById"
			parameterType="int"
			resultType="com.moonback.pilgrimage.model.dto.MemberDto">
		SELECT
            member_id,
            email,
            password,
            nickname,
            role_id,
            status_id,
            created_at
        FROM member
        WHERE member_id = #{memberId}
	</select>
	
	<select id="countByEmail"
			parameterType="string"
			resultType="int"
	>
		SELECT COUNT(*)
        FROM member
        WHERE email = #{email}	
	</select>
	
	<insert id="insertMember"
			parameterType="MemberDto"
			useGeneratedKeys="true"
			keyProperty="memberId"
			>
		INSERT INTO member(
			email,
			password,
			nickname,
			role_id,
			status_id
		)
		VALUES (
			#{email},
			#{password},
			#{nickname},
			#{roleId},
			#{statusId}
		)
	</insert>
	
	<update id="updateMember">
		UPDATE member
		<set>
			<if test="email != null">
				email = #{email},
			</if>
			<if test="nickname != null">
				nickname = #{nickname},
			</if>
			<if test="password != null">
				password = #{password}
			</if>
		</set>
		WHERE member_id = #{memberId}
	</update>
	
	<update id="withdrawById"
			parameterType="int">
		UPDATE member
		SET status_id = 2
		WHERE member_id = #{memberId}
	</update>
</mapper>
