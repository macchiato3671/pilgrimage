<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.pilgrimage.model.mapper.MemberMapper">
	<select id="findByEmail"
			parameterType="string"
			resultType="com.ssafy.pilgrimage.model.dto.MemberDto">
		SELECT
            member_id,
            email,
            password,
            nickname,
            role_id,
            status_id,
            created_at
        FROM Member
        WHERE email = #{email}
	</select>
	
	<select id="findById"
			parameterType="int"
			resultType="com.ssafy.pilgrimage.model.dto.MemberDto">
		SELECT
            member_id,
            email,
            password,
            nickname,
            role_id,
            status_id,
            created_at
        FROM Member
        WHERE member_id = #{memberId}
	</select>
	
	<select id="countByEmail"
			parameterType="string"
			resultType="int"
	>
		SELECT COUNT(*)
        FROM Member
        WHERE email = #{email}	
	</select>
	
	<insert id="insertMember"
			parameterType="MemberDto"
			useGeneratedKeys="true"
			keyProperty="memberId"
			>
		INSERT INTO MEMBER(
			email,
			password,
			nickname,
			role_id,
			status_id
		)
		VALUES (
			#{email},
			#{password},
			#{nickname},
			#{roleId},
			#{statusId}
		)
	</insert>
</mapper>