<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.moonback.pilgrimage.model.mapper.PlanMapper">
	<select
			id="selectPlans"
			resultType="com.moonback.pilgrimage.model.dto.TravelPlanRowDto"
			>
		SELECT *
		FROM TravelPlan
		WHERE member_id = #{memberId}
		ORDER BY updated_at DESC
		LIMIT #{offset}, #{pageSize};
	</select>

	<select
			id="selectPlan"
			resultType="com.moonback.pilgrimage.model.dto.TravelPlanRowDto"
			>
		SELECT *
		FROM TravelPlan
		WHERE member_id = #{memberId}
			AND plan_id = #{planId};
	</select>

	<select
			id="selectPlanDetails"
			resultType="com.moonback.pilgrimage.model.dto.TravelPlanDetailRowDto"
			>
		SELECT *
		FROM PlanDetail
		WHERE plan_id = #{planId}
		ORDER BY day_no ASC, begin_time ASC;
	</select>

	<insert
			id="insertPlan"
			>
		INSERT INTO TravelPlan(
			member_id,
			title,
			begin_date,
			end_date,
			memo
		)
		VALUES(
			#{memberId},
			#{title},
			#{beginDate},
			#{endDate},
			#{memo}
		)
	</insert>

</mapper>
